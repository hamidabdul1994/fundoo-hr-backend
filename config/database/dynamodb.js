// // set region if not set (as not set by the SDK by default)
// var AWS = require('aws-sdk'),
// dyn = new AWS.DynamoDB({ endpoint: new AWS.Endpoint('http://localhost:8888') });
//
// dyn.listTables(function (err, data){
//    console.log('listTables->', err, ' data->' ,data);
// });
//
//
// var AWS = require('aws-sdk');
// var config = {"endpoint":"http://localhost:8000"};
// var client = new AWS.DynamoDB(config);

// 
// var AWS = require('aws-sdk');
// var config = {"endpoint":"http://localhost:8888"};
// var client = new AWS.S3(config);
// dyn = new AWS.DynamoDB({ endpoint: new AWS.Endpoint(config) });
// // console.log(dyn);
//
//
// var AWS = require('aws-sdk');
// var config = {"endpoint":"http://localhost:8888"};
// var client = new AWS.DynamoDB(config);
